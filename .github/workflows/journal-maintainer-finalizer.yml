name: Atlas Journal Maintainer Finalizer

on:
  workflow_run:
    workflows: ["Atlas CI"]
    types: [completed]

permissions:
  contents: read
  pull-requests: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - name: probe
        run: echo hi
