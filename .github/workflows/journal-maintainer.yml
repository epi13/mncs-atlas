name: Atlas Journal Maintainer

on:
  schedule:
    - cron: "17 17 * * 5"

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  journal:
    runs-on: ubuntu-latest
    steps:
      - name: probe
        run: echo hi
