#!/usr/bin/env python3
"""Small source-checkout launcher for ``python -m registry``."""

from pathlib import Path
import sys

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT))

from registry.__main__ import main  # noqa: E402


raise SystemExit(main())
